import 'package:flutter/material.dart';

class EventEditPage extends StatelessWidget {
  const EventEditPage({super.key, required event});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}